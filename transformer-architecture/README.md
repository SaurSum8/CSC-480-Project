The files contained in this folder are not directly used in our app.
They exist just to demonstrate the seq2seq transformer model used, with our initial configuration, when we first attempted to train it.<br><br>
The actual fine-tuned (and more organized) model used can be found in the "transformer" directory.
